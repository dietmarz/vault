include::{root}/.inc/include.adoc[]

= Show And Tell

== Änerung: Parameter aufgeteilt: 

Allgemeiner Teil:
[source, yaml]
----
webclient:
  ** attribute:
  ** schema:
  ** structures:
  ** contextPath: /schema/structures/contexts
  ** definitions:
  ** contexts:
  ** contextPath: /schema/definitions/contexts
----

Umgebungsabhängiger Teil:
[source, yaml]
----
webclient:
attribute:
rootUrl: https://attribute.develop.prime.cloud.otto.de
----

Warum:
[source, kotlin]
----

abstract class IntegrationTest {
  ** @DynamicPropertySource
  ** @JvmStatic
  ** fun registerDynamicAttributeSchemaEndpointUrls(registry: DynamicPropertyRegistry) {
  ** // So ist es richtig
  ** registry.add("webclient.attribute.rootUrl") { mockServerContainer.endpoint }
  **
  ** // So war es, gleichzeitig mehrere unterschiedliche URL-Pfade nicht möglich
  ** registry.add("webclient.attribute.schema.structures.endpointUrl") { mockServerContainer.endpoint }
  ** }
}
----
