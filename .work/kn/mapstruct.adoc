include::{root}/.inc/include.adoc[]

= Mapstruct

* Source: doc/mapstructreference-guide.pade[Reference]
* Own mapstruct project. Running only in KN Environment

== Mapping resolution

[source, java]
----
@Mapping(target = "tmsInstructions", source = "shipmentOrder.orderInformation.handlingText")
EdiordKnieMessage map(EDIORD ediord); // Main map method

// Typen die aufeiander treffen aus dem @Mapping Beispiel:
// EdiordKnieMessage.TmsInstructions from EDIORD.SHPORD.ORDINF.List<FreeText>
// Erzwingt die Existenz von: (Woher sollte mapstruct wissen wie man das sonst mapped)

default TmsInstructions mapTmsInstructions(List<FreeText> freeTexts) {
  ** return mapTmsInstructions(1, freeTexts);
}
// Erzwingt
---- 

== Learning Hints
* Stolen from EdiordKnieMessage0320MapperImpl
* Statt Interfaces kann man auch abstrakte Klassen benutzen, die sogar Member haben dürfen. Das kann man evt für caching
  ** benutzen.*
